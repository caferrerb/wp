# ===========================================
# WhatsApp Receiver - Environment Variables
# ===========================================

# Server Configuration
PORT=3000
NODE_ENV=development

# WhatsApp Session
WA_SESSION_PATH=./wa_session

# Database
DATABASE_PATH=./data/messages.db

# ===========================================
# Email Configuration
# ===========================================
# Provider: 'mailpit' (local testing) or 'mailersend' (production)
EMAIL_PROVIDER=mailpit

# Sender Configuration
EMAIL_FROM=noreply@localhost
EMAIL_FROM_NAME=WhatsApp Receiver
EMAIL_REPORT_TO=your-email@example.com

# MailerSend Configuration (only needed if EMAIL_PROVIDER=mailersend)
# Get your API key from: https://www.mailersend.com/
# EMAIL_FROM must use a verified domain (e.g., noreply@your-domain.mlsender.net)
MAILERSEND_API_KEY=

# Mailpit Configuration (only needed if EMAIL_PROVIDER=mailpit)
# Mailpit UI available at: http://localhost:8025
MAILPIT_URL=http://mailpit:8025

# ===========================================
# Daily Report Scheduler
# ===========================================
DAILY_REPORT_ENABLED=false
DAILY_REPORT_HOUR=8
DAILY_REPORT_MINUTE=0

# Filter: only include messages from these phone numbers (comma-separated)
# Leave empty to include all messages
EMAIL_FILTER_NUMBERS=

# Timezone for scheduling
TZ=America/Bogota
